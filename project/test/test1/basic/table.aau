import console;

/*==================================
1.table属性
===================================*/

// 注意：1.key不要加引号, 2分隔符是;号, 3key不能有-符
var tab = { 1; 2; key="value"; 3; arrs = {k="v"; "a"}};

console.log(tab[1], tab[2], tab[3]);  // 1, 2, 3

console.log(tab.key, tab["key"]);  // 此两种方法相同
//console.log( tab["data-text"]);   // 有-符号时只能使用此方法使用

var childtab = tab["arrs"];
console.varDump(childtab);

console.pause();

/*==================================

===================================*/